App 自动化 python+appium 版
1、在App 自动化平台选择过程中，一共提供了3各版本，分别是RF、Appium、Airtest三个版本，这是 基于Appium+python，采用PO分层设计；
2、自动读取连接的App设备，根据设备数量生成服务端启动命令，并根据每台设备生成对应的报告；
3、每次执行前清除数据；执行后关闭服务；
4、支持日志、数据参数化；
4、这个版本需要一定代码基础；
5、同样，仅适用于Android；IOS待开发；
